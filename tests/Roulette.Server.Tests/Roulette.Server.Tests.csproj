<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="coverlet.collector" Version="6.0.4" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.14.1" />
    <PackageReference Include="xunit" Version="2.9.3" />
    <PackageReference Include="xunit.runner.visualstudio" Version="3.1.4" />

    <!--
      Referenced here with its runtime assets, unlike Roulette.Server, which excludes
      them so the mod zip does not ship a second copy of SPT's own assemblies into
      the server process. The test host has no server around it and needs the real
      thing on disk to load the types at all.
    -->
    <PackageReference Include="SPTarkov.Server.Core" Version="4.1.2" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
    <Using Include="Roulette.Server" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Roulette.Server\Roulette.Server.csproj" />
  </ItemGroup>

</Project>
